model Message {
  id         Int      @id @default(autoincrement())
  senderId   Int
  receiverId Int
  listingId  Int
  content    String
  createdAt  DateTime @default(now())
  isRead     Boolean  @default(false)
}
